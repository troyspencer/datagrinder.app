syntax = "proto3";

service Grinder {
    // Returns a grind result
    rpc Grind (GrinderInput) returns (GrinderOutput) {}
}

message GrinderInput {
    int32 Setting = 1;
    int32 Height = 2;
    int32 Width = 3;
}

message GrinderOutput {
    string Base64image = 1;
}